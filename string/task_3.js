let str = "education";

let lowerStr = str.toLowerCase();

if (
  lowerStr.includes("a") &&
  lowerStr.includes("e") &&
  lowerStr.includes("i") &&
  lowerStr.includes("o") &&
  lowerStr.includes("u")
) {
  console.log("string contains all the vowels a, e, i, o, u");
}
else{
console.log("No vowel conntain");
}
